<% if @user %>
  Welcome, <%= @user.email %>
<% end %>
<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL: <%= link.url %>
      Tags: <% link.tags %>
    </li>
  <% end %>
</ul>

<a href="/users/new">Sign up</a>
<a href="/login">Sign in</a>
